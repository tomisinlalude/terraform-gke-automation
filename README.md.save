# My Project

My project is an open-source three-tier e-commerce app. Visit the URLs here:

[Admin Dashboard](https://github.com/tomisinlalude/saleor-dashboard)
[API](https://github.com/tomisinlalude/saleor-api)
[Storefront](https://github.com/tomisinlalude/saleor-storefront)

# Deployment

## Automate the creation of a K8s cluster with Terraform

Launch and manage a GKE cluster using Terraform.

## Launch GKE Cluster

```
$ terraform init
$ terraform plan
$ terraform apply
```

## Connect cluster to the application

Login to gcloud
Connect cluster to your app

```
gcloud container clusters get-credentials saleor --zone europe-west1-c --project generated-media-310804
```
### Using Helm Chart in each tier
 
Gen
erating the kubernetes files using helm chart
```
helm create <chart-name>
```
Update the yaml files to suit the project
Install the objects: helm install saleor ./saleor-chart/
Upgrade the chart in the cluster when changes are made: 
helm upgrade saleor ./saleor-chart/
Create IP address in cluster: gcloud compute addresses create
saleor-api --global
Get the IP address created from Ingress: kubectl get ingress

